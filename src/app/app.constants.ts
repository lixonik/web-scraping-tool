/**
 * Тексты шаблона `app.html` (корневого компонента).
 */
export const APP_TEMPLATE_VARIABLES = {
  dividers: {
    browser: 'Browser',
    element: 'Element',
    tools: 'Tools',
    logs: 'Logs',
  },
} as const;
